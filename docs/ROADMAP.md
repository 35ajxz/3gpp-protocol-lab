# 工程路线图

当前工程已经不再保留示例化最小链路，后续只围绕两条主线推进：

## 一、真实规范接入主线

目标：把 `TS 38.331` 的真实文档稳定转成可校验、可复用的协议模型。

当前已完成：
- 真实文档导入
- 过程级切片
- 大模型结构化提取
- ASN.1 提取与编解码校验
- 运行时产物生成

下一步重点：
1. 扩大已覆盖的 `5.3.x` 过程范围。
2. 增强状态机提取的一致性评估。
3. 补充更多消息类型的编码样本。

## 二、逻辑漏洞挖掘主线

目标：从“消息是否能解析”继续推进到“协议状态机是否存在逻辑漏洞”。

当前已完成：
- 参考执行模型
- 候选目标对比
- 逻辑规则加载
- 序列扰动
- 异常分类与轨迹落盘

下一步重点：
1. 把本地候选目标替换成真实协议处理器。
2. 增加最小复现序列压缩能力。
3. 接入在线 OAI 注入环境。

## 三、当前工程边界

- 当前只覆盖少量关键过程，不是全量规范。
- 逻辑漏洞测试仍以离线执行和离线对比为主。
- 形式化验证还没有接入真正的外部求解器。
